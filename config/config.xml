<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<root>
	<config>
		<!-- socket or com -->
		<type>socket</type>
	</config>
	
	<com>
		<!-- 串口号，注意：大小写敏感 -->
		<port>COM3</port>
		<!-- 波特率 -->
		<baudrate>9600</baudrate>
		<!-- 数据位 -->
		<databit>8</databit>
		<!-- 停止位 -->
		<stopbit>1</stopbit>
		<!-- 校验位 -->
		<parity></parity>
	</com>
	
	<socket>
		<!-- server or client -->
		<!-- 填写server或client -->
		<type>client</type>
		<!-- if server, set null -->
		<!-- 若为server，则此节点留空（需要保留节点） -->
		<ip>192.168.6.11</ip>
		<!-- 若为server，则此节点为监听端口；若为client，则此节点为连接服务器的端口 -->
		<port>1035</port>
		<!-- 是否长连接, true或者false -->
		<keepAlive>true</keepAlive>
	</socket>

	<protocol config="config\protocol-config-tower.xml">
		<interval time="30000" protocol="alarm_report" />
		<!-- <interval time="30000" protocol="status" />
		<interval time="5000" protocol="alarm" /> -->
		<response>
			<field>15,18</field>
			<value>00001f40</value>
			<head>23646C686A7478787923</head>
			<class>byte</class>
			<pro>status</pro>
			<quote>
				<field name="id,id_desc">39,42</field>
				<rule>response_id.rule</rule>
			</quote>
		</response>
	</protocol>
</root>